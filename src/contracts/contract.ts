import { NetworkOption } from '../core/network';

export function transactionData(address: string, amount: string, networkOption: NetworkOption): string {
    return `0x...`; // Имитация данных транзакции
}